{
  "name": "vonage-zapier",
  "version": "1.0.0",
  "description": "Zapier integration for Vonage APIs — SMS, Messages, Voice, Verify, Number Insight",
  "main": "index.js",
  "scripts": {
    "test": "jest --testPathPattern=test",
    "validate": "zapier validate",
    "push": "zapier push",
    "deploy": "zapier push && zapier promote"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "uuid": "^9.0.1",
    "zapier-platform-core": "18.6.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  },
  "private": true
}
