node_modules/
.env
*.pem
*.key
build/
.zapierapprc

# Demo media — kept in git history but excluded from the Zapier build (size)
docs/demo/*.gif
docs/demo/*.mp4
docs/demo/*.png
docs/demo/voice-samples/

# Internal working notes & demo assets — local only
notes/
.DS_Store

# Obsidian vault config (local)
.obsidian/
